
public interface UserAuth {
	
	public boolean authenticate(String user, String pass);
	
	/*
	 * methods to check if username is in certain database
	 * 
	 * will implement when time comes
	 */
	
	
}
